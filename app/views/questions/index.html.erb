<p id="notice"><%= notice %></p>

<h3>
    Listing Questions for :- Ruby
</h3>

<table border=2>
    <thead>
        <tr>
            <th>Title</th>
            <th>Status</th>
            <th>Level</th>
            <th>Question category</th>
            <th colspan="3"></th>
        </tr>
    </thead>

    <tbody>
        <% @questions.where('question_category_id == ?', 1).each do |question| %>
        <tr>
            <td><%= question.title %></td>
            <td><%= question.status %></td>
            <td><%= question.level %></td>
            <td><%= question.question_category.name %></td>
            <td><%= link_to 'Show', question %></td>
            <td><%= link_to 'Edit', edit_question_path(question) %></td>
            <td><%= link_to 'Destroy', question, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
        <% end %>
    </tbody>
</table>

<br>

<h3>
    Listing Questions for :- Rails
</h3>
<table border=2>
    <thead>
        <tr>
            <th>Title</th>
            <th>Status</th>
            <th>Level</th>
            <th>Question category</th>
            <th colspan="3"></th>
        </tr>
    </thead>

    <tbody>
        <% @questions.where('question_category_id == ?', 2).each do |question| %>
        <tr>
            <td><%= question.title %></td>
            <td><%= question.status %></td>
            <td><%= question.level %></td>
            <td><%= question.question_category.name %></td>
            <td><%= link_to 'Show', question %></td>
            <td><%= link_to 'Edit', edit_question_path(question) %></td>
            <td><%= link_to 'Destroy', question, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
        <% end %>
    </tbody>
</table>
<br>

<%= link_to 'New Question', new_question_path %>
